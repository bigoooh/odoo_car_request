<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="car_request.list">
      <field name="name">car_request list</field>
      <field name="model">car_request.car_request</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="car_request.action_window">
      <field name="name">car_request window</field>
      <field name="res_model">car_request.car_request</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="car_request.action_server">
      <field name="name">car_request server</field>
      <field name="model_id" ref="model_car_request_car_request"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="car_request" id="car_request.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="car_request.menu_1" parent="car_request.menu_root"/>
    <menuitem name="Menu 2" id="car_request.menu_2" parent="car_request.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="car_request.menu_1_list" parent="car_request.menu_1"
              action="car_request.action_window"/>
    <menuitem name="Server to list" id="car_request" parent="car_request.menu_2"
              action="car_request.action_server"/>
    -->
  </data>
</odoo>